# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.mjs"
# should be updated accordingly.
#
# Secrets may live in `t4sg-c4p-2026/.env` or the parent `t4sg-c4p/.env`.
# Next.js and Electron load parent first, then this folder (local wins on conflicts).

# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://abcdefghijklmnopqrst.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="longlonglongstring"
SECRET_SUPABASE_CONNECTION_STRING="postgresql://postgres:YourDatabasePasswordHere@db.abcdefghijklmnopqrst.supabase.co:5432/postgres"

# Anthropic — used for /api/chat, Electron screenshot vision (main.cjs), and image_processing.ts
ANTHROPIC_API_KEY="sk-ant-..."
# Vision in Electron defaults to claude-haiku-4-5. Use Sonnet/Opus for higher-quality chat or bbox.
# ANTHROPIC_MODEL="claude-haiku-4-5"
# ANTHROPIC_MODEL_TILE_FAST / TILE_REFINE — override tile presence vs bbox model (default: ANTHROPIC_MODEL)
# ANTHROPIC_MODEL_TILE_FAST="claude-haiku-4-5"
# ANTHROPIC_MODEL_TILE_REFINE="claude-haiku-4-5"
# High-zoom crops (< ~35% of screen): Sonnet + higher token budget (override default claude-sonnet-4-20250514)
# ANTHROPIC_MODEL_TILE_PRECISE="claude-sonnet-4-20250514"
# Set to false to use a single expensive vision call per tile (no fast pass)
# NEXT_PUBLIC_VISION_TILE_TWO_STAGE=true

# Vision UI: show coordinate grid + chunk debug panel in Electron (set to true)
# NEXT_PUBLIC_VISION_DEBUG=true

# OpenAI — optional; only for /api/transcribe and /api/speak (Anthropic has no STT/TTS)
# OPENAI_API_KEY="sk-..."
# Optional TTS overrides (defaults: gpt-4o-mini-tts + marin, with automatic fallback to tts-1 + nova)
# OPENAI_TTS_MODEL="tts-1"
# OPENAI_TTS_VOICE="nova"
